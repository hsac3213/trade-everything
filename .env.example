# PostgreSQL
POSTGRESS_PASSWORD=changeme

# Frontend Host (WebAuthn SSL Certificate CN)
# 로컬: localhost, 원격: IP 주소나 도메인
FRONTEND_HOST=localhost

# Backend Host (프론트엔드에서 백엔드 접속용)
BACKEND_HOST=localhost
BACKEND_PORT=8001
BACKEND_PROTOCOL=http

# WebAuthn 설정 (Passkey 인증용)
# RP_ID: WebAuthn Relying Party ID (일반적으로 도메인명, IP 주소는 불가)
# RP_ORIGIN: 프론트엔드 접속 URL
# 주의: IP 주소로는 WebAuthn이 작동하지 않습니다. localhost 또는 도메인 필요
RP_ID=localhost
RP_NAME=Trade Everything
RP_ORIGIN=http://localhost:5173